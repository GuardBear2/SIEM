# Run from Sources

The GuardBear Server can be run with three possible methods:
- **GuardBear Server Binary** at `/usr/share/guardbear-server/bin/guardbear-server`.
- **Systemd** with the `systemctl start guardbear-server` command.
- **SysV init** with the `service guardbear-server start` command.